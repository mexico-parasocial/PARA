/**
 * BetaUserStorageSync
 *
 * Placeholder for the beta-user storage sync pass. Mounted near the root of
 * the app (see App.tsx / App.web.tsx) so it can reconcile persisted beta
 * state once the session is available. Currently a no-op — implement the
 * sync logic here and keep it renderless.
 */
export function BetaUserStorageSync() {
  return null
}
